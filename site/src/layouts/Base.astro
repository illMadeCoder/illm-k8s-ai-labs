---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Kubernetes benchmark results from the K8s AI Lab' } = Astro.props;
const base = import.meta.env.BASE_URL;

const navLinks = [
  { label: 'Home', href: base },
  { label: 'Projects', href: `${base}projects/` },
  { label: 'Categories', href: `${base}categories/` },
  { label: 'Comparisons', href: `${base}comparisons/` },
  { label: 'About', href: `${base}about/` },
];

const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="color-scheme" content="dark light" />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <title>{title}</title>
    <script is:inline>
      (function() {
        var theme = localStorage.getItem('theme');
        if (theme === 'light') {
          document.documentElement.classList.remove('dark');
        }
      })();
    </script>
  </head>
  <body class="min-h-screen flex flex-col bg-bg text-text">
    <header class="border-b border-border">
      <nav class="max-w-6xl mx-auto px-3 sm:px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-8">
            <a href={base} class="font-bold text-lg text-text hover:text-accent no-underline transition-colors">
              Testbed UI
            </a>
            <div class="hidden md:flex items-center gap-6">
              {navLinks.map((link) => (
                <a
                  href={link.href}
                  class:list={[
                    'text-sm no-underline transition-colors',
                    currentPath === link.href || currentPath.startsWith(link.href.replace(/\/$/, '') + '/')
                      ? 'text-accent font-semibold'
                      : 'text-text-muted hover:text-accent',
                  ]}
                >
                  {link.label}
                </a>
              ))}
            </div>
          </div>
          <div class="flex items-center gap-4">
            <span class="hidden lg:inline text-xs text-text-muted font-mono">Automated K8s Benchmarking Lab</span>
            <a
              href="https://github.com/illmadecoder/k8s-ai-cloud-testbed"
              class="text-sm text-text-muted hover:text-accent no-underline"
              target="_blank"
              rel="noopener"
            >
              GitHub
            </a>
            <button id="theme-toggle" class="text-text-muted hover:text-accent transition-colors" aria-label="Toggle theme">
              <svg id="theme-icon-sun" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg id="theme-icon-moon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>
            <button id="mobile-menu-btn" class="md:hidden text-text-muted hover:text-accent" aria-label="Toggle menu">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
        <div id="mobile-menu" class="hidden md:hidden mt-4 pb-2 border-t border-border pt-4">
          <div class="flex flex-col gap-3">
            {navLinks.map((link) => (
              <a
                href={link.href}
                class:list={[
                  'text-sm no-underline',
                  currentPath === link.href || currentPath.startsWith(link.href.replace(/\/$/, '') + '/')
                    ? 'text-accent font-semibold'
                    : 'text-text-muted hover:text-accent',
                ]}
              >
                {link.label}
              </a>
            ))}
          </div>
        </div>
      </nav>
    </header>
    <slot name="breadcrumb" />
    <main class="max-w-6xl mx-auto px-3 sm:px-6 py-8 w-full flex-1">
      <slot />
    </main>
    <footer class="mt-auto border-t border-border">
      <div class="max-w-6xl mx-auto px-3 sm:px-6 py-6 text-center text-sm text-text-muted">
        <p>
          Built with <a href="https://astro.build" class="hover:text-accent" target="_blank" rel="noopener">Astro</a> +
          <a href={`${base}about/`} class="hover:text-accent">experiment-operator</a>
        </p>
        <p class="mt-1">
          <a
            href="https://github.com/illmadecoder/k8s-ai-cloud-testbed"
            class="hover:text-accent"
            target="_blank"
            rel="noopener"
          >
            github.com/illmadecoder/k8s-ai-cloud-testbed
          </a>
        </p>
      </div>
    </footer>
    <!-- Early visitor welcome modal -->
    <div id="welcome-modal" class="hidden" style="position:fixed;inset:0;z-index:9999;display:none;align-items:center;justify-content:center;background:rgba(0,0,0,0.6);backdrop-filter:blur(4px);padding:1rem;">
      <div style="max-width:28rem;width:100%;border-radius:10px;padding:2rem;font-family:var(--font-mono);position:relative;background:var(--bg-surface);border:1px solid var(--border);">
        <p style="font-size:1.1rem;margin:0 0 1rem;line-height:1.6;color:var(--text);">
          Hi friend! You're one of the early ones to this page. Bear with me while I generate this frontend and pull my history of experiments in.
        </p>
        <p style="font-size:0.9rem;margin:0 0 1.5rem;line-height:1.5;color:var(--text-muted);">
          Come back in a month to see the real thing :)
        </p>
        <p style="font-size:0.8rem;margin:0 0 1.5rem;color:var(--text-muted);">
          &mdash; Jesse Bergerstock, 2/14/2026
        </p>
        <button id="welcome-close" style="width:100%;padding:0.6rem;border:1px solid var(--accent);border-radius:6px;background:transparent;color:var(--accent);font-family:var(--font-mono);font-size:0.85rem;cursor:pointer;transition:background 0.15s,color 0.15s;">
          Got it!
        </button>
      </div>
    </div>

    <script is:inline>
      (function () {
        // Welcome modal — show once per visitor
        var modal = document.getElementById('welcome-modal');
        var closeBtn = document.getElementById('welcome-close');
        if (modal && !localStorage.getItem('welcome-dismissed')) {
          modal.classList.remove('hidden');
          modal.style.display = 'flex';
        }
        if (closeBtn && modal) {
          closeBtn.addEventListener('mouseenter', function () {
            closeBtn.style.background = 'var(--accent)';
            closeBtn.style.color = 'var(--bg)';
          });
          closeBtn.addEventListener('mouseleave', function () {
            closeBtn.style.background = 'transparent';
            closeBtn.style.color = 'var(--accent)';
          });
          closeBtn.addEventListener('click', function () {
            modal.style.display = 'none';
            localStorage.setItem('welcome-dismissed', '1');
          });
        }

        var btn = document.getElementById('mobile-menu-btn');
        var menu = document.getElementById('mobile-menu');
        if (btn && menu) {
          btn.addEventListener('click', function () {
            menu.classList.toggle('hidden');
          });
        }

        var toggle = document.getElementById('theme-toggle');
        var sunIcon = document.getElementById('theme-icon-sun');
        var moonIcon = document.getElementById('theme-icon-moon');
        function updateIcons() {
          var isDark = document.documentElement.classList.contains('dark');
          if (sunIcon) sunIcon.classList.toggle('hidden', !isDark);
          if (moonIcon) moonIcon.classList.toggle('hidden', isDark);
        }
        updateIcons();
        if (toggle) {
          toggle.addEventListener('click', function () {
            var isDark = document.documentElement.classList.toggle('dark');
            localStorage.setItem('theme', isDark ? 'dark' : 'light');
            updateIcons();
          });
        }
      })();
    </script>
  </body>
</html>
