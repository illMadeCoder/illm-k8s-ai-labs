# ArgoCD values for Kind hub cluster (bootstrap only)
#
# Used by: task kind:bootstrap
# After bootstrap, ArgoCD manages itself via platform/hub/values/argocd.yaml
#
# ENVIRONMENT CONFIG: If changing Kind network, also update:
#   - platform/hub/manifests/metallb-config/kustomization.yaml (POOL_RANGE)
#   - platform/hub/values/argocd.yaml (loadBalancerIP)
#   - platform/hub/values/dns-stack.yaml (loadBalancerIP)
#   - platform/hub/cluster/Taskfile.yaml (DNS_IP)

server:
  service:
    type: LoadBalancer
    loadBalancerIP: 172.19.255.201  # ArgoCD gets .201, DNS gets .200
