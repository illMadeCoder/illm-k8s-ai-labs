# SeaweedFS S3 credentials for Argo Workflows artifact storage
# SeaweedFS uses anonymous auth — the Secret satisfies Argo's
# artifact repository schema requirement.
apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-creds
  namespace: argo-workflows
type: Opaque
stringData:
  accessKey: "any"
  secretKey: "any"
