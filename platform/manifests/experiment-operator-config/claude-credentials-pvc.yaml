apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-credentials-pvc
  namespace: experiment-operator-system
  labels:
    app: experiment-analyzer
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-retain
  resources:
    requests:
      storage: 1Mi
