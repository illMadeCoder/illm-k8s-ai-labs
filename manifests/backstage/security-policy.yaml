apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: backstage-basic-auth
  namespace: backstage
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: backstage
  basicAuth:
    users:
      name: backstage-basic-auth
